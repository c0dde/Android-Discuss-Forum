<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yswong.discussion">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LiveChatActivity"></activity>
        <activity android:name=".SelectForumActivity" />
        <activity android:name=".WebViewActivity" />
        <activity
            android:name=".YoutubeActivity"
            android:label="@string/title_activity_youtube"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ThreadContentActivity" />
        <activity android:name=".ViewThreadActivity" />
        <activity
            android:name=".NewReplyActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".NewThreadActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".IndexActivity" />
        <activity
            android:name=".RegisterActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".SignInActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>